# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ManyLittles (iOS)' do
  platform :ios, '15.1'
  use_frameworks!
  pod 'Alamofire'
end

target 'ManyLittles (macOS)' do
  platform :macos, '12.0'
  use_frameworks!
  pod 'Alamofire'
end
